<!DOCTYPE html>

<title>Hello AdminControllerImagesController!</title>

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>File list for knifes</h1>
    <ul>
        {% for kfile in klist %}
            <li>{{kfile}}</li>
        {% endfor %}
    </ul>
    <h1>File list for slideshow</h1>
    <ul>
        {% for sfile in slist %}
            <li>{{sfile}}</li>
        {% endfor %}
    </ul>
</div>
{% endblock %}
