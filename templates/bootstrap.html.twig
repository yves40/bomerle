<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8" />
        <meta http-equiv="X-UA-Compatible" content="IE=edge" />
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
        <meta name="description" content="" />
        <meta name="author" content="" />
        <title>
            {% block title %}
                Static Navigation - SB Admin
            {% endblock %} 
        </title>
    </head>
    <body>
        <nav class="navbar navbar-expand navbar-dark bg-dark">
            <!-- Navbar Brand-->
            <a class="navbar-brand ps-3" href="{{ path('home')}}">{% trans %}app.version{% endtrans %}</a>
            <!-- Sidebar Toggle button -->
            <button class="btn btn-link btn-sm order-1 order-lg-0 me-4 me-lg-0" id="sidebarToggle" href="#"><i class="fas fa-bars"></i></button>
            <!-- Navbar-->
            <ul class="navbar-nav me-auto me-md-0 me-3 me-lg-4">
                <!-- lang Toggle -->
                {% if locale == 'fr' %}
                    <a href="{{ path('bootadmin.switch', { locale: 'en'}) }}" class="nav-link"><img src="/images/uk16.png" alt=""></a>
                {% else %}
                    <a href="{{ path('bootadmin.switch', { locale: 'fr'}) }}" class="nav-link"><img src="/images/fr16.png" alt=""></a>
                {% endif %}                        
                <!-- user info if connected -->
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" id="navbarDropdown" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        {% if app.user %}
                            {{ app.user.email }}
                        {% else %}
                            <i class="fas fa-user fa-fw"></i>
                        {%  endif %}
                    </a>
                    <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
                        {% if app.user %}
                            <li><a class="dropdown-item" href="{{ path('app_logout') }}">{% trans %}user.disconnect{% endtrans %}</a></li>
                        {% else %}
                            <li><a class="dropdown-item" href="{{ path('app_login') }}">{% trans %}user.connect{% endtrans %}</a></li>
                        {%  endif %}
                    </ul>
                </li>
            </ul>
        </nav>
        <div id="layoutSidenav">
            <div id="layoutSidenav_nav">
                <nav class="sb-sidenav accordion sb-sidenav-dark" id="sidenavAccordion">
                    <div class="sb-sidenav-menu">
                        <div class="nav">
                            {# ----------------------------------------------------------------------------------- #}
                            {# The general menu starts here #}
                            <div class="sb-sidenav-menu-heading">{% trans %}admin.tools{% endtrans %}</div>
                            <a class="nav-link" href="{{ path('bootadmin.home') }}">
                                <div class="sb-nav-link-icon"><i class="fas fa-house"></i></div>
                                {% trans %}home.home{% endtrans %}
                            </a>
                            {% if app.user %}
                                <a class="nav-link" href="{{ path('app_logout') }}">
                                    <div class="sb-nav-link-icon"><i class="fas fa-square-xmark"></i></div>
                                    {% trans %}user.disconnect{% endtrans %}
                                </a>
                            {% else %}
                                <a class="nav-link" href="{{ path('app_login') }}">
                                    <div class="sb-nav-link-icon"><i class="fas fa-right-to-bracket"></i></div>
                                    {% trans %}user.connect{% endtrans %}
                                </a>
                            {% endif %}
                            {# ----------------------------------------------------------------------------------- #}
                            {# The action menu starts here #}
                            {% if app.user %} 
                                <div class="sb-sidenav-menu-heading">{% trans %}admin.actions{% endtrans %}</div>
                                <a class="nav-link collapsed" href="#" data-bs-toggle="collapse" data-bs-target="#collapseKnives" aria-expanded="false" aria-controls="collapseLayouts">
                                    <div class="sb-nav-link-icon"><i class="fas fa-columns"></i></div>
                                    {% trans %}admin.knives{% endtrans %}
                                    <div class="sb-sidenav-collapse-arrow"><i class="fas fa-angle-down"></i></div>
                                </a>
                                <div class="collapse" id="collapseKnives" aria-labelledby="headingOne" data-bs-parent="#sidenavAccordion">
                                    <nav class="sb-sidenav-menu-nested nav">
                                        {% if is_granted('ROLE_ADMIN') %}
                                            <a class="nav-link" href="#">{% trans %}admin.listknives{% endtrans %}</a>
                                            <a class="nav-link" href="#">{% trans %}admin.addknife{% endtrans %}</a>
                                            <a class="nav-link" href="#">{% trans %}admin.accessories{% endtrans %}</a>
                                            <a class="nav-link" href="{{ path('bootadmin.categories', { "new" : "true" } )}}">{% trans %}admin.categories{% endtrans %}</a>
                                            <a class="nav-link" href="{{ path('bootadmin.handles', { "new" : "true" } )}}">{% trans %}admin.handles{% endtrans %}</a>
                                            <a class="nav-link" href="{{ path('bootadmin.mechanisms', { "new" : "true" } )}}">{% trans %}admin.mechanisms{% endtrans %}</a>
                                            <a class="nav-link" href="{{ path('bootadmin.metals', { "new" : "true" } )}}">{% trans %}admin.metals{% endtrans %}</a>
                                        {% endif %}                                
                                    </nav>
                                </div>
                                <a class="nav-link collapsed" href="#" data-bs-toggle="collapse" data-bs-target="#collapseEvents" aria-expanded="false" aria-controls="collapseLayouts">
                                    <div class="sb-nav-link-icon"><i class="fas fa-columns"></i></div>
                                    {% trans %}admin.events.events{% endtrans %}
                                    <div class="sb-sidenav-collapse-arrow"><i class="fas fa-angle-down"></i></div>
                                </a>
                                <div class="collapse" id="collapseEvents" aria-labelledby="headingOne" data-bs-parent="#sidenavAccordion">
                                    <nav class="sb-sidenav-menu-nested nav">
                                        {% if is_granted('ROLE_ADMIN') %}
                                            <a class="nav-link" href="#">{% trans %}admin.events.list{% endtrans %}</a>
                                        {% endif %}                                
                                    </nav>
                                </div>
                                <a class="nav-link collapsed" href="#" data-bs-toggle="collapse" data-bs-target="#collapseLayouts" aria-expanded="false" aria-controls="collapseLayouts">
                                    <div class="sb-nav-link-icon"><i class="fas fa-columns"></i></div>
                                    Users
                                    <div class="sb-sidenav-collapse-arrow"><i class="fas fa-angle-down"></i></div>
                                </a>
                                <div class="collapse" id="collapseLayouts" aria-labelledby="headingOne" data-bs-parent="#sidenavAccordion">
                                    <nav class="sb-sidenav-menu-nested nav">
                                        {# <a class="nav-link" href="{{ path('user.list') }}">List all</a> #}
                                        <a class="nav-link" href="#">{% trans %}user.list{% endtrans %}</a>
                                        {% if is_granted('ROLE_ADMIN') %}
                                            <a class="nav-link" href="#">{% trans %}user.add{% endtrans %}</a>
                                        {% endif %}                                
                                    </nav>
                                </div>
                            {% endif %}
                        </div>
                    </div>
                </nav>
            </div>
            <div id="layoutSidenav_content">
                <main>
                    <div class="container-fluid px-4">
                    {% block pagetitle %}
                        <h1 class="mt-4">bootstrap template</h1>
                    {% endblock %}
                        <div class="card mb-4">
                            <div class="card-body">
                                <!-- The flash message section -->  
                                {% for message in app.session.flashBag.get('info') %}
                                    <div class="alert alert-info">{{message}}</div>
                                {% endfor %}
                                {% for message in app.session.flashBag.get('success') %}
                                    <div class="alert alert-success">{{message}}</div>
                                {% endfor %}
                                {% for message in app.session.flashBag.get('error') %}
                                    <div class="alert alert-danger">{{message}}</div>
                                {% endfor %}
                                <!-- The page main content -->  
                                <p class="mb-0">
                                    {% block body %}
                                        bootstrap template body
                                    {% endblock %}
                                </p>
                            </div>
                        </div>
                        <div style="height: 100vh"></div>
                        <div class="card mb-4"><div class="card-body">When scrolling, the navigation stays at the top of the page. This is the end of the static navigation demo.</div></div>
                    </div>
                </main>
                <footer class="py-4 bg-light mt-auto">
                    <div class="container-fluid px-4">
                        <div class="d-flex align-items-center justify-content-between small">
                            <div class="text-muted">Copyright &copy; Your Website 2022</div>
                            <div>
                                <a href="#">Privacy Policy</a>
                                &middot;
                                <a href="#">Terms &amp; Conditions</a>
                            </div>
                        </div>
                    </div>
                </footer>
            </div>
        </div>
        {% block javascripts %}
        {% endblock %}
    </body>

    <link rel="stylesheet" href="{{ asset('css/mini.css') }}">
    <link rel="stylesheet" href="{{ asset('node_modules/startbootstrap-sb-admin/dist/css/styles.css') }}">

    <script src="{{asset("node_modules/bootstrap/dist/js/bootstrap.bundle.min.js")}}"></script>
    <script src="{{asset("node_modules/startbootstrap-sb-admin/dist/js/scripts.js")}}"></script>

    {# <script src="{{asset("node_modules/fontawesome-free/js/all.js")}}"></script> #}

    <script src="{{ asset('node_modules/jquery/dist/jquery.min.js')}}"></script>

    <script src="{{ asset('scripts/scripts.js')}}"></script>
    <script src="{{ asset('scripts/properties.js')}}"></script>
    <script src="{{ asset('scripts/timeHelper.js')}}"></script>
    <script src="{{ asset('scripts/images.js')}}"></script>


    {# <script src="{{asset("node_modules/bootstrap/dist/js/bootstrap.min.js")}}"></script> #}
    {# <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">²    <link href={{asset("node_modules/bootstrap/dist/css/bootstrap.css")}} rel="stylesheet" /> #}
    {# <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js" crossorigin="anonymous"></script> #}
    {# <script src="{{asset("node_modules/bootstrap/dist/js/bootstrap.bundle.min.js")}}"></script> #}
    <script src="https://use.fontawesome.com/releases/v6.1.0/js/all.js" crossorigin="anonymous"></script>

</html>
